fastapi>=0.110,<1
uvicorn>=0.29,<1
Werkzeug>=3.1.3,<4
# Trusted local worker only (encrypted token store); never ships to cPanel —
# requirements-cpanel.txt stays dependency-free. >=50 clears the 2026
# advisories (PYSEC-2026-35/2141/3552/3553/3554, GHSA-537c-gmf6-5ccf);
# cp39-abi3 wheels cover 3.9+ interpreters.
cryptography>=50,<51
