fastapi>=0.110,<1
uvicorn>=0.29,<1
Werkzeug>=3.1.3,<4
# Trusted local worker only (encrypted token store); never ships to cPanel —
# requirements-cpanel.txt stays dependency-free. <45 keeps wheels for the
# system Python 3.9 used by run.sh.
cryptography>=42,<45
